import MainRoutes from './MainRoutes';

import './index.scss';

const Body = () => (
  <div className="main-content" id="mainContent">
    <main>
      <MainRoutes />
    </main>
  </div>
);

export default Body;
